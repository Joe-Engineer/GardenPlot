<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>GardenPlot.DataFetcher</RootNamespace>
    <AssemblyName>GardenPlot.DataFetcher</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reuse the model types (PlantProfile, PaletteItem, PaletteCatalog). -->
    <ProjectReference Include="..\GardenPlotWeb\GardenPlotWeb.csproj" />
  </ItemGroup>

</Project>
